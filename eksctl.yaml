apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: project-cluster
  region: il-central-1
  version: "1.31"

autoMode:
  enabled: true
  # Optional: specify explicit IAM role or let eksctl manage it
